# FastTodo

Минималистичный todo-менеджер: задачи с именованными пунктами-чекбоксами, секциями, прогресс-баром и двумя режимами просмотра.

## Возможности

### Задачи и пункты
- **Задачи** — создание, удаление, переименование, сортировка перетаскиванием
- **Пункты** — чекбоксы с произвольными названиями; пустое имя → `Пункт №1`, `Пункт №2`, …
- **Секции** — группировка пунктов с заголовком, счётчиком `выполнено/всего`, сворачиванием; пункты вне секций добавляются формой внизу списка
- **Прогресс** — процент выполнения и анимированный бар (толчок при отметке, confetti при 100%)
- **Две вкладки** — «Текущая» (детальный вид) и «Все задачи» (список карточек)

### Интерфейс
- **Навигация** — переключение между задачами кнопками с названиями соседних задач
- **Темы** — цветовые схемы из `static/css/themes/` (dark, gruvbox, nord, dracula, catppuccin, tokyo-night, adeptus-mechanicus и др.); список и стили подгружаются автоматически
- **Drag-and-drop** — пункты (внутри и между секциями), секции, задачи; placeholder показывает место вставки; при перетаскивании развёрнутые пункты и секции сворачиваются
- **Текст пунктов** — авто-раскрытие многострочного текста при наведении и редактировании
- **Память** — последняя открытая задача и выбранная тема в `localStorage`; свёрнутые секции тоже запоминаются

## Стек

| Слой | Технология |
|------|------------|
| Backend | Flask |
| База данных | SQLite (`todo.db`) |
| Frontend | HTML, CSS (модули + темы), vanilla JS (ES modules) |

## Структура проекта

```
fasttodo/
├── app.py              # Flask-приложение и REST API
├── database.py         # Инициализация и миграции SQLite
├── themes.py           # Обнаружение тем и сборка CSS-бандла
├── icon.png            # Favicon
├── requirements.txt
├── templates/
│   └── index.html      # Единственная страница
└── static/
    ├── css/
    │   ├── style.css   # Сборка стилей (темы — через /api/themes.css)
    │   ├── tokens.css, base.css, layout.css, …
    │   └── themes/     # Файлы цветовых схем (*.css)
    └── js/
        ├── main.js     # Точка входа
        ├── api/        # HTTP-клиент
        ├── core/       # state, constants, storage
        ├── data/       # tasks, progress
        └── ui/         # render, items, sections, drag-drop, theme, effects
```

## Запуск

```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Откройте [http://127.0.0.1:5000](http://127.0.0.1:5000).

При первом запуске создаётся `todo.db` в корне проекта.

### Параметры

```bash
python app.py --host 0.0.0.0 --port 5000   # доступ из сети
python app.py --debug                       # только с --host 127.0.0.1 (по умолчанию)
```

`--debug` на публичном адресе заблокирован: debug-консоль Werkzeug небезопасна.

## Как это устроено

### База данных

Три таблицы:

- `tasks` — `id`, `title`, `position`, `created_at`
- `items` — `id`, `task_id`, `name`, `completed`, `position`, `section_id` (nullable)
- `sections` — `id`, `task_id`, `title`, `position`

Порядок задач, секций и пунктов хранится в `position`. Пункт может быть вне секций (`section_id = NULL`) или внутри одной секции.

### API

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/api/themes` | Список тем: `{ id, name, color? }` |
| `GET` | `/api/themes.css` | Динамический `@import` всех файлов из `themes/` |
| `GET` | `/api/tasks` | Список задач с пунктами, секциями и прогрессом |
| `POST` | `/api/tasks` | Создать задачу |
| `GET` | `/api/tasks/:id` | Одна задача |
| `PATCH` | `/api/tasks/:id` | Переименовать задачу |
| `DELETE` | `/api/tasks/:id` | Удалить задачу |
| `PUT` | `/api/tasks/reorder` | Порядок задач (`{ "order": [3, 1, 2] }`) |
| `POST` | `/api/tasks/:id/items` | Добавить пункт (`section_id` опционально) |
| `PUT` | `/api/tasks/:id/layout` | Порядок пунктов и секций |
| `POST` | `/api/tasks/:id/sections` | Создать секцию с пункта (`from_item_id`) |
| `POST` | `/api/tasks/:id/sections/ungroup-from` | Вынести пункты из секции |
| `PATCH` | `/api/sections/:id` | Переименовать секцию |
| `DELETE` | `/api/sections/:id` | Удалить секцию |
| `PATCH` | `/api/items/:id` | Отметить / переименовать пункт |
| `DELETE` | `/api/items/:id` | Удалить пункт |

### Frontend

Одностраничное приложение без сборщика. Точка входа — `static/js/main.js` (ES modules).

Глобальное состояние в `core/state.js`:

- `tasks` — кэш с сервера
- `currentTaskId` — выбранная задача (`localStorage`: `fasttodo:lastTaskId`)
- `view` — активная вкладка

При переключении чекбокса состояние обновляется оптимистично — список пунктов не перерисовывается целиком, чтобы не сбивать hover и анимации.

Секции рендерятся блоками с заголовком; layout (порядок пунктов и секций, принадлежность к секции) читается из DOM и сохраняется через `PUT /layout`.

### Темы

Темы не зашиты в HTML: при загрузке страницы `ui/theme.js` запрашивает `GET /api/themes` и строит выпадающий список. Активная тема — атрибут `data-theme` на `<html>`; выбор сохраняется в `localStorage` (`fasttodo:theme`).

Стили тем подключаются через `style.css` → `@import url("/api/themes.css")`. Эндпоинт генерирует `@import` для каждого `*.css` в `static/css/themes/` (логика в `themes.py`). Новый файл в этой папке автоматически попадает и в меню, и в бандл — правки `index.html` или `style.css` не нужны.

#### Метаданные в файле темы

В начале файла — шебанг-комментарий:

```css
/*! theme: Nord | color: #88c0d0 */
[data-theme="nord"] {
  --bg: #2e3440;
  ...
}
```

| Поле | Обязательно | Описание |
|------|-------------|----------|
| `theme: …` | нет | Отображаемое имя в меню. Если не указано — имя файла без `.css` |
| `color: …` | нет | Цвет подписи в списке тем. Если не указано — обычный `text-muted` активной темы |

`id` темы совпадает с именем файла (например `nord.css` → `data-theme="nord"`).

#### CSS-переменные

Каждая тема задаёт набор custom properties: `--bg`, `--surface`, `--text`, `--accent`, `--success`, `--danger` и др. Общие токены (радиусы, переходы) — в `tokens.css`.

**Важно:** не используйте селектор `:root` в файлах тем — он всегда применяется к `<html>` и перебивает другие темы по порядку загрузки. Только `[data-theme="…"]`.

#### Добавление темы

1. Создайте `static/css/themes/my-theme.css` с шебангом и блоком `[data-theme="my-theme"] { … }`.
2. Перезагрузите страницу — тема появится в меню.

Меню тем закрывается при клике вне его, по Escape и когда кнопка выбора уходит из видимости при прокрутке списка пунктов.

## Лицензия

[GLWTPL](LICENSE) (Good Luck With That Public License) — полный текст в файле [`LICENSE`](LICENSE).

Проект полностью навайбкожен: собран в ходе диалога с ИИ во время заучивания билетов на экзамен без тщательного ревью, тестов и гарантий пригодности. Используйте как есть.
